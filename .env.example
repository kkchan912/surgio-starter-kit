# Surgio 环境变量配置示例
# 复制此文件为 .env 并填入你的实际值（本地开发用）
# 在 Netlify 部署时，在后台设置这些环境变量

# 管理员访问令牌（拥有所有权限）
ACCESS_TOKEN=your-secret-access-token-here

# 访客令牌（只读权限）
VIEWER_TOKEN=your-viewer-token-here

# 注意：
# 1. 这些令牌用于保护你的配置文件
# 2. 建议使用强密码或随机字符串
# 3. 不要将实际的 .env 文件提交到 Git
# 4. Netlify 部署时在后台的 Environment variables 中设置